services:
  - type: web
    name: link-funnel
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: linkdb
          property: connectionString

databases:
  - name: linkdb
    plan: free
